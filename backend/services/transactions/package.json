{
  "name": "transactions-service",
  "version": "1.0.0",
  "description": "Transactions service for multivendor marketplace",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node dist/index.js",
    "dev": "nodemon --exec ts-node --esm index.ts",
    "db:up": "mysql -u root -p < migrations/create_tables.sql",
    "db:down": "mysql -u root -p -e 'DROP TABLE IF EXISTS wallet_transactions, subscriptions, subscription_plans, wallets, transactions;'",
    "test": "echo \"Tests not implemented yet\""
  },
  "dependencies": {
    "express": "^4.18.0",
    "mysql2": "^3.0.0",
    "cors": "^2.8.5",
    "helmet": "^6.0.0",
    "morgan": "^1.10.0",
    "jsonwebtoken": "^9.0.0",
    "bcryptjs": "^2.4.3",
    "stripe": "^12.0.0",
    "paypal-rest-sdk": "^1.8.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}